# ForgeRL — HuggingFace Space requirements
# (CPU-only: no GPU/training deps)

# Core models & config
pydantic>=2.0.0
pyyaml>=6.0
python-dotenv>=1.0.0

# Web server
fastapi>=0.110.0
uvicorn>=0.27.0

# UI
gradio>=4.44.0

# Utilities
rich>=13.0.0
matplotlib>=3.8.0
numpy>=1.24.0
httpx>=0.27.0

# Optional: real LLM sub-agents (Space runs in simulated mode if key absent)
google-generativeai>=0.8.0
pyaudioop>=0.3.1
